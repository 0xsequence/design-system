import {
  Meta,
  Title,
  IconGallery,
  IconItem,
} from '@storybook/addon-docs/blocks'
import { ThemeProvider } from '../../providers/ThemeProvider/ThemeProvider.js'
import * as icons from '../../icons/index.js'

<Meta title="Design System/Iconography" />

# Iconography

Collection of icons used in the design system.

<ThemeProvider defaultTheme="light" storageKey="storybook-preview-theme">
  <IconGallery>
    {Object.entries(icons).map(([key, Icon]) => (
      <IconItem name={key} key={key}>
        <Icon />
      </IconItem>
    ))}
  </IconGallery>
</ThemeProvider>
