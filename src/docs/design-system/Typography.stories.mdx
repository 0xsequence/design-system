import { Meta, Typeset } from '@storybook/addon-docs'

import { Text } from '../../components'
import { tokens } from '../../tokens'

export const sampleText = 'The quick brown fox jumps over the lazy dog'

<Meta title="Design System/Typography" />

# Typography

The defined set of typefaces and used in the design system.

**xlarge**

<Text variant="xlarge">{sampleText}</Text>

**large**

<Text variant="large">{sampleText}</Text>

**medium**

<Text variant="medium">{sampleText}</Text>

**normal**

<Text variant="normal">{sampleText}</Text>

**small**

<Text variant="small">{sampleText}</Text>

**xsmall**

<Text variant="xsmall">{sampleText}</Text>

**code**

<Text variant="code">{sampleText}</Text>
